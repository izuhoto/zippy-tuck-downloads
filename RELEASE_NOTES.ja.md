# ZippyTuck v1.0.0 2026-08-03

ZippyTuck v1.0.0 の正式リリースです。
モーダル操作をノーマルモードなしの直接操作へ刷新し、対象切り替え、セッション内 undo / redo、ウィンドウ mark、グリッドモード、ヘルプ、URL スキームを追加しました。

## 主な変更

- 起動ショートカットから直接 move mode に入り、`e` / `r` で move / resize を切り替え、`Enter` 1 回で確定、`Esc` でセッション中に触った全ウィンドウを開始時へ戻すよう変更
- `Option+Tab` / `Option+Shift+Tab` の対象スイッチャーを追加し、セッション中に操作対象ウィンドウを選択できるようにした
- `u` / `Ctrl+r` によるセッション内のウィンドウ単位バッチ undo / redo を追加
- 52 スロット（`a-z` / `A-Z`）のウィンドウ mark を追加し、単窓の更新、全窓レイアウト保存・適用、可視化、削除、プレビュー巡回に対応
- `Ctrl+w` から入るグリッドモードを追加し、セル選択、ウィンドウ割当、自動割当、複数セルスパン、境界調整、形状 bookmark、形状プレビュー・復元に対応
- メニューバーのデータメニューから mark / グリッド形状の実行・削除・エクスポート・インポート・全クリアを行えるようにした
- `?` とメニューから開けるヘルプオーバーレイを追加し、概要、move、resize、grid、quick、URL スキームのページと URL コピーを提供
- `zippytuck://` URL スキームを追加し、mark 適用、グリッド形状適用、セッション開始、ヘルプ、設定を外部から起動可能にした
- mode HUD がセッション中に常駐し、一定時間後の不透明度を設定できるよう改善。全窓レイアウト適用前の確認も設定でオン / オフ可能にした
- グリッドのデフォルト分割設定を `2x2`・`1...100` に見直し、設定値とセッション中の分割変更を切り分け

# ZippyTuck v0.0.3 2026-07-29

ZippyTuck v0.0.3 のプレリリースです。
Global Quick Ops で移動とリサイズを組み合わせたあとも、`Esc` の一括取り消しが開始時の位置とサイズまで戻るよう修正しました。

## 改善・修正

- Global Quick Ops で「移動 → リサイズ」または「リサイズ → 移動」のあとに `Esc` を押したとき、位置とサイズの両方が quick 操作開始時へ戻るよう修正
- サイズ変更を含む復元で、対象アプリが `AXPosition` / `AXSize` の片方を反映しない場合でも着地しやすいよう、復元時のフレーム書き込み順序と間隔を見直し
- 修飾キー解除後の 1 秒 linger 中に同じ quick 操作を握り直した場合、処理が遅れても一括取り消しの基準フレームを維持するよう改善
- `AXEnhancedUserInterface` が有効なアプリでの復元安定性を改善
- `Esc` 復元の環境依存問題を実機で切り分ける `restore_probe` を追加し、既存の AX probe を `tools/probes` 配下へ整理

# ZippyTuck v0.0.2 2026-07-29

ZippyTuck v0.0.2 のプレリリースです。
常駐アプリの多い Mac で、起動ショートカット後の HUD 表示やキーボード操作が数秒待たされる問題を改善しました。

## 改善・修正

- 常駐アプリが多い環境でも、起動ショートカットからの HUD 表示と move / resize モードのキーボード操作が素早く応答するよう改善
- スナップ候補の収集対象を画面上のウィンドウに絞り、AX 通信のタイムアウトを設定して応答しないアプリによる待ち時間を抑制
- キーボードの相対移動・リサイズで、打鍵ごとにスナップ候補を再収集しないようキャッシュ利用を見直し
- 起動時 HUD をフェード待ちなしで即時表示し、重い処理の直後でも見えにくくなることを防止

# ZippyTuck v0.0.1 2026-07-27

ZippyTuck v0.0.1 のプレリリースです。
正式リリース前の初期バージョンとして、macOS のウィンドウ移動・リサイズをキーボードとマウスで素早く行うための基本機能を利用できます。

## 主な機能

- メニューバー常駐型の macOS ウィンドウ移動・リサイズアプリ
- 起動ショートカットから入る ZippyTuck normal / move / resize mode
- `h` / `j` / `k` / `l`、数字プレフィックス、画面端移動、グリッド操作によるキーボード中心のウィンドウ操作
- `zh` / `zj` / `zk` / `zl` や `z` + マウス移動による方向スナップ
- 修飾キー + マウス移動で通常状態から直接使える Global Quick Ops
- 連続移動、連続リサイズ、方向スナップ移動、方向スナップリサイズ
- アクティブウィンドウを画面四隅へ配置するショートカット
- 操作中の HUD 表示と対象ウィンドウのハイライト
- 起動ショートカット、Global Quick Ops、グリッド、言語、スナップ関連設定の変更
- 日本語・英語表示と、システム言語に合わせた表示切り替え
- 初回起動時の利用規約・免責事項への同意、About 表示、Web 配布版の更新確認
